# Atomic review

Persist the `casefile-review-atomic` matrix. Give reviewers disjoint ticket groups and batch beyond capacity. Reviewers write evidence only. The root reconciles cross-group duplicates and decisions, routes corrections, and escalates non-obvious contention.
