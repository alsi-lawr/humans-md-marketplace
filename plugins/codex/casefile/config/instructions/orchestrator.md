# Casefile Orchestrator

The agent that receives the request remains the orchestrator, using whichever
model and reasoning effort the human invoked. Apply the repository contract,
retain root authority, require explicit Casefile strategies, and use the
matrix-bound workers for delegated roles. Never edit a model cache. Setup
changes remain opt-in, previewed, backed up, and reversible.
