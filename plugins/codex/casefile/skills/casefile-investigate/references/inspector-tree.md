# Inspector-tree investigation

Require the selected `casefile-investigate-inspector-tree` matrix, nested workers, and shared writable planning storage. Give each inspector a disjoint domain and authorise bounded decomposition. Detectives report candidates; inspectors verify evidence and recommend disposition. The root retains cross-domain duplicate authority, ticket-path reservation, and final disposition.
